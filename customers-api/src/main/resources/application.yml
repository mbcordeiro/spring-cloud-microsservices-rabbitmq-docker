spring:
  application:
    name: customers-api

server:
  port: 0

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    instance:
      instance-id: ${s[ring.application.name}:${spring.application.instance_id:${random.value}}